/** Header/nav mark: full calendar lockup, cropped tight for 32px. */
export const BRAND_ICON_SRC = "/icons/classa-header.png";
/** Full calendar lockup for login, register, and slow-load. */
export const BRAND_LOGO_SRC = "/icons/classa-logo-256.png";
export const BRAND_NAME = "classa";

/** Show the slow-load fallback when boot/auth stays pending this long. */
export const SLOW_LOAD_TIMEOUT_MS = 8_000;
